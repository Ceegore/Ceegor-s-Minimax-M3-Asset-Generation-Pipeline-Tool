# ---- dependencies ----
node_modules/

# ---- archived workspace cleanup ----
backup/

# ---- runtime assets (downloaded by `npm run setup`) ----
bin/

# ---- runtime state (per-user) ----
config.txt
batches.json
state.json
providers.json
generated/
output/
test-output.txt

# ---- build artifacts ----
dist/
dist-build/
dist-out/
dist-final/
dist-verify/
out/
build/
ReleaseGithub/
*.exe
*.dmg
*.AppImage
*.snap
*.deb
*.rpm
*.zip
*.tar.gz

# ---- test / debug output ----
test-*.log
*.log

# ---- test coverage + e2e screenshots ----
# Coverage reports (line / UI-surface / IPC) are generated artifacts.
coverage/
# Visual regression baselines (QA-031: committed so CI can enforce diffs).
# tests/visual-baselines/ — no longer ignored
# E2E screenshots live in os.tmpdir(), but never let a stray run drop them here.
mmx-shots-*/
*.png
!assets/icon.png
!tests/visual-baselines/*.png

# ---- IDE / editor ----
.idea/
.vscode/
.dbg/
.e2e-shots/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---- environment / secrets ----
.env
.env.local
.env.*.local
*.pem
*.key
# RQ-002: signing material must never live inside the release worktree —
# an untracked minisign.pub would dirty `git status --porcelain` and make
# the build provenance fail strict verification. The release workflow keeps
# the pinned key under RUNNER_TEMP (MINISIGN_PUB_PATH); a TRACKED pinned
# key would be committed explicitly and never via this rule.
minisign.pub

# ---- dev scratch ----
_*.md
_*.js
_*.txt
_temp*.md
_repro_*.js
_verify_*.js
commit_msg_*.txt
settings.json
test_out/

# verification scratch leftovers (probe fixtures + run logs)
scratch_verify/_tmp*/
scratch_verify/_*.log
scratch_verify/_*.txt
test-run.txt

# release-qualification bundle extractions (evidence input, not source)
_rq*/
dist-work/
